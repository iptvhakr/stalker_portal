{% extends 'layout.twig' %}

{% set title = 'video-club' %}
{% set list = '<li><a href="#" data-filter="0"><span>Все</span></a></li>' %}

{% block content %}
{#    <div class="row header_title">
        <div class="col-xs-10 col-sm-2">
            <h3>Реклама</h3>
        </div>
    </div>#}
    <div id="iptv_list">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <a href="{{ app.request.baseUrl }}/{{ app.controller_alias }}/add-video-ads" class="btn btn-success pull-right">Добавить</a>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                {% if app['ads'] %}
                    <div class="box">
                        <div class="box-content">
                            <div class="box-content no-padding">
                                <table class="table table-striped table-bordered table-hover table-datatable" id="datatable-1">
                                    <thead>
                                        <tr>
                                            <th data-filter="title">Название</th>
                                            <th data-filter="url">Адрес</th>
                                            <th data-filter="weight">Вес</th>
                                            <th data-filter="started">Просмотр начат</th>
                                            <th data-filter="ended">Просмотр завершен</th>
                                            <th data-filter="must_watch">Необходимо посмотреть</th>
                                            <th data-filter="status">Статус</th>
                                            <th data-filter="operations">Действия</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <!-- Start: list_row -->
                                        {% for item in app.ads %}
                                            <tr data-adsid="{{ item.id }}">
                                                <td ><a href="#">{{ item.title }}</a></td>
                                                <td data-filter="url">{{ item.url }}</td>
                                                <td data-filter="weight">{{ item.weight }}</td>
                                                <td data-filter="started">{{ item.started }}</td>
                                                <td data-filter="ended">{{ item.ended }}</td>
                                                <td data-filter="must_watch">{{ item.must_watch }}</td>
                                                <td data-filter="status">{% if item.status %}<span class="txt-success">Опубликовано</span>{% else %}<span class="txt-danger">Не опубликовано</span>{% endif %}</td>
                                                <td data-filter="operations">
                                                    <div class="col-xs-3 col-sm-8">
                                                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">                    
                                                            <i class="pull-right fa fa-cog"></i>
                                                        </a>
                                                        <ul class="dropdown-menu pull-right">
                                                            <li>
                                                                <a href="{{ app.request.baseUrl }}/{{ app.controller_alias }}/edit-video-ads?id={{ item.id }}">
                                                                    <span>Изменить</span>
                                                                </a>
                                                            </li>
                                                            <li>
                                                                <a class="main_ajax" href="{{ app.request.baseUrl }}/{{ app.controller_alias }}/toggle-video-ads-status" data-adsstatus="{% if item.status %}0{% else %}1{% endif %}" data-adsid="{{ item.id }}">
                                                                    <span>{% if item.status %}Отключить{% else %}Включить{% endif %}</span>
                                                                </a>
                                                            </li>
                                                            <li>
                                                                <a class="main_ajax" href="{{ app.request.baseUrl }}/{{ app.controller_alias }}/remove-video-ads" data-adsid="{{ item.id }}">
                                                                    <span>Удалить</span>
                                                                </a>
                                                            </li>
                                                        </ul>
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                        <!-- End: list_row -->
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
<script type="text/javascript" defer>

    function TestTable1() {
        $('#datatable-1').dataTable({
            "language": {
                "url": "./plugins/datatables/lang/ru_RU.json"
            },
            "bFilter": true,
            "bPaginate": true,
            "bInfo":     true,
            "aoColumnDefs": [ 
                { className: "action-menu", "targets": [ -1 ] },
                { "targets": [ -1 ], "orderable": false},
                { "targets": [ 1, 2, 3, 4, 5, 6, 7], "searchable": false},

            ]
        });
    }
    function yelp() {
        $(document).ready(function () {
            LoadDataTablesScripts(TestTable1);
            
            $(document).on('click', "a.main_ajax", function (e) {
                e.stopPropagation();
                e.preventDefault();
                var _this = $(this);

                if (!_this.data('atcion')) {
                    $.ajax({
                        url: $(this).attr('href'),
                        type: 'POST',
                        data: $(this).data(),
                        success: function (data) {
                            if (data.success) {
                                for (var key in data) {
                                    _this.data(key, data[key]);
                                }
                            } else {
                                alert('Some server error');
                            }
                        },
                        error: function (data) {
                            var errAction = '';
                            if (typeof(data.responseJSON) == 'object') {
                                errAction +=  data.responseJSON.action + 'Error';
                                for (var key in data.responseJSON) {
                                    _this.data(key, data.responseJSON[key]);
                                }
                            }
                            if ($.isFunction(window[errAction])) {
                                window[errAction]($(_this));
                            } else {
                                alert('Some network error or access denied');
                            }
                        },
                        dataType: "json",
                        async: false
                    });
                } 

                if ($.isFunction(window[$(this).data('action')]) && !$(this).data('error')) {
                    window[$(this).data('action')]($(this));
                }
                _this.closest('div.open').removeClass('open');
                return false;
            });
        });
    }
    
    document.addEventListener("DOMContentLoaded", yelp, false);
    
    var removeAds = function(obj){
        var cRow = $(obj).closest('tr');
        $('#datatable-1').DataTable().row(cRow).remove().draw( false );
    }
    
    var toggleVideoAdsStatus = function(obj){
        $(obj).text($(obj).data('title'));
        $(obj).closest('tr').find('td[data-filter="status"]').html($(obj).data('status'));
    }
</script>
{% endblock %}